def twoSum(nums: list, target: int) -> list:
    #output = []
    #for num in nums:
    #    check = target - num
    #    if check in nums:
    #        output.append(nums.index(num))
    Indices = [ nums.index(num) for num in mums ]

                
    return output
